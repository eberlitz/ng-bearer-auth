describe('$auth', function() {
    beforeEach(module('ngBearerAuth'));

    // beforeEach(inject(['$window', '$httpBackend', '$auth', 
    // 	function($window, $httpBackend, $auth) {
    //     this.$window = $window;
    //     this.$httpBackend = $httpBackend;
    //     this.$auth = $auth;
    // }]));

    // it('should be defined', function() {
    //     expect(this.$auth).toBeDefined();
    // });

});